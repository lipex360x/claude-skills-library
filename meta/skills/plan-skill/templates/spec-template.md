# Skill Spec: {{skill-name}}

> Generated by `/plan-skill` on {{date}}. This spec is self-contained — a fresh session can read it and understand every decision without prior context.

## Meta

| Field | Value |
|-------|-------|
| **Name** | `{{skill-name}}` |
| **Plugin** | `{{plugin-name}}` ({{plugin-description}}) |
| **User-invocable** | `{{true/false}}` |
| **Input sources** | {{document / conversation / verbal / args}} |
| **Output** | {{what the skill produces — file, message, side effect}} |

## Purpose

### What it does

{{One paragraph describing the core action — what the skill does when invoked.}}

### Why it exists

{{One paragraph explaining the gap this skill fills — what problem exists without it, what motivated its creation.}}

### What it does NOT do

{{Explicit scope boundaries — what a user might expect but this skill intentionally excludes. Prevents scope creep during implementation.}}

## Trigger

### Description draft

{{Pushy description following the pattern: WHAT it does + WHEN to activate + "even if they don't explicitly say X". This becomes the frontmatter `description` field.}}

### Activation phrases

| Phrase | Type |
|--------|------|
| `/{{skill-name}}` | Exact command |
| {{natural phrase 1}} | Natural language |
| {{natural phrase 2}} | Natural language |
| {{indirect reference}} | Indirect |

## Input Contract

### Required inputs

| Name | Source | Validation | Example |
|------|--------|------------|---------|
| {{name}} | {{args / environment / file / conversation}} | {{validation rule}} | {{concrete example}} |

### Optional inputs

| Name | Default | Description | Example |
|------|---------|-------------|---------|
| {{name}} | {{default value}} | {{what it controls}} | {{concrete example}} |

### Input parsing rules

{{How the skill determines which input source to use when multiple are available. Priority order, fallback behavior, conflict resolution.}}

## Workflow

### Step 1 — {{title}}

**Action:** {{What this step does}}

**Inputs:** {{What it reads}}

**Outputs:** {{What it produces}}

**Failure path:** {{What happens if this step fails — specific recovery action, not "handle error"}}

### Step 2 — {{title}}

**Action:** {{What this step does}}

**Inputs:** {{What it reads — including outputs from previous steps}}

**Outputs:** {{What it produces}}

**Failure path:** {{What happens if this step fails}}

### Step N — {{title}}

{{Continue as needed. Each step must have Action, Inputs, Outputs, and Failure path.}}

## Output Contract

### Format

{{Exact structure of the output — file format, sections, frontmatter schema if applicable.}}

### Location

{{Where the output is written — file path pattern, configurable or fixed.}}

### Post-output actions

{{What happens after output is generated — presented to user, consumed by another skill, committed to git, etc.}}

## Dependencies

### External tools

| Tool | Required | Purpose | Fallback if unavailable |
|------|----------|---------|------------------------|
| {{tool name}} | {{yes/no}} | {{why needed}} | {{what to do without it}} |

### File system reads

| Path | Purpose | Required |
|------|---------|----------|
| {{path relative to project root}} | {{what it reads and why}} | {{yes/no}} |

### File system writes

| Path | Purpose | Created or updated |
|------|---------|-------------------|
| {{path relative to project root}} | {{what it writes}} | {{created / updated}} |

## Cross-Skill State

### Reads from other skills

| Skill | What it reads | Path/Format | What if missing |
|-------|--------------|-------------|-----------------|
| {{skill-name}} | {{artifact or state}} | {{where to find it}} | {{behavior when absent}} |

### Writes for other skills

| Skill | What it writes | Path/Format | Contract |
|-------|---------------|-------------|----------|
| {{skill-name}} | {{artifact or state}} | {{where it goes}} | {{format the consuming skill expects}} |

### Shared resources

{{List any shared files (project-settings.json, STRUCTURE.md, CLAUDE.md) that this skill reads or writes. For each: what it reads/writes, and whether concurrent access is a concern.}}

## Guardrails

### Must do

| Rule | Reason |
|------|--------|
| {{mandatory behavior}} | {{why — not just "best practice" but the consequence of violating it}} |

### Must NOT do

| Rule | Reason |
|------|--------|
| {{forbidden behavior}} | {{why — what goes wrong if violated}} |

### Idempotency

{{What happens if the skill is run twice with the same input? Does it overwrite, append, skip, or ask? Specify the expected behavior.}}

### Resource cost

{{Token budget awareness — does this skill load large files, spawn subagents, make API calls? Estimate the cost profile: light (<2k tokens), medium (2-10k), heavy (>10k).}}

## Decisions Log

{{Every decision made during planning — both inferred and user-confirmed. This section is the audit trail that explains WHY the spec looks the way it does.}}

| # | Decision | Classification | Rationale |
|---|----------|---------------|-----------|
| 1 | {{what was decided}} | {{[inferred] / [ask] / [gap]}} | {{why this choice — context, trade-offs, user input}} |
| 2 | {{what was decided}} | {{[inferred] / [ask] / [gap]}} | {{why this choice}} |

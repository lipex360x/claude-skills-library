# {skill-name}

{One-line description of what the skill does.}

## Trigger phrases

- "{trigger phrase 1}"
- "{trigger phrase 2}"
- "{trigger phrase 3}"
- Also activates when {contextual trigger — even without explicit keywords}

## How it works

1. **{Phase 1 title}** — {Brief description of what happens}
2. **{Phase 2 title}** — {Brief description of what happens}
3. **{Phase 3 title}** — {Brief description of what happens}

{Optional: key design decisions, approval gates, or notable behavior.}

## Usage

```
/{skill-name} {arguments if applicable}
```

{Example scenario showing typical input and what the skill produces.}

## Directory structure

```
{skill-name}/
├── SKILL.md              # Core instructions
├── references/           # {Brief note on what's here, if present}
└── templates/            # {Brief note on what's here, if present}
```

## Installation

```bash
npx skills add {repo-url} --skill {skill-name}
```

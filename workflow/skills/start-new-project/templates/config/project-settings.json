{
  "_comment": "Template: project settings (JSON) | IMPORTANT: This is a living document — update 'pages' whenever new routes are created so Claude can navigate and verify them via CDP",
  "pr-merge-to": "main",
  "chrome": {
    "cdp": "http://localhost:9222",
    "launchCommand": ".claude/start-chrome.sh",
    "baseUrl": "http://localhost:3000",
    "testPort": 3100,
    "tabs": {
      "app": "http://localhost:3000"
    },
    "pages": {
      "home": "/",
      "login": "/login",
      "dashboard": "/dashboard"
    }
  }
}
